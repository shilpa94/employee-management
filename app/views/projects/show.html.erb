<div class="container">
    <%#= f.collection_select(:employee_id, Employee.all, :id, :name, {:prompt => "Please Select a employee"}, {:multiple => true}) %>
    <%#= collection_select(:employee, :employee_id, Employee.all, :id, :first_name) %>
    <%= form_tag(assign_employee_project_path(@project), method: :post) do %>
      <%= select_tag "employee_id", options_from_collection_for_select(Employee.all, "id", "firstname"), multiple: true,prompt:"select employee",style: "width: 200px;" %>
      <%= submit_tag %>
    <% end %>
</div>
